# Web 3D 数字风洞模拟器（F-35）— 完整开发提示词 v2

## 项目概述

请开发一个**高质量的交互式 Web 3D 数字风洞模拟器**。该应用在浏览器中实时展示一架 **F-35 战斗机**在风洞中的气流变化、空气动力学现象和关键参数。要求具有**物理逻辑、参数联动和专业航空仪器界面**，而非简单的粒子动画。

**目标用户**：航空爱好者、飞行学员、初等空气动力学教学场景。
**⚠️ 工程边界**：本模拟采用**简化空气动力学模型**，仅用于**教育和可视化展示**，**不可用于真实飞行测试、工程认证或性能评估**（此免责必须在 UI 底部始终可见）。

---

## 一、技术栈与约束

按优先级使用：

* **Three.js**（核心渲染）
* **WebGL 2.0 / WebGPU**（WebGPU 优先，带 WebGL 2.0 fallback）
* **GLSL / WGSL Shader**（气流粒子、压力云图、体素渲染、纹影）
* **HTML5 / CSS3 / JavaScript 或 TypeScript**
* **Vite** 作为构建工具（按需）
* **不使用任何后端服务**（纯前端 SPA / 静态托管）

---

## 二、整体场景

创建一个**大型透明风洞测试舱**。测试舱内放置一架**高细节 F-35A 战斗机模型**，飞机通过风洞支架固定在测试区域中央。

场景元素：

* 透明或半透明风洞舱壁（带菲涅尔反射）
* 风洞入口和收缩段
* 测试段（主观测区域）
* 扩散段
* 大型风扇或气流发生装置（叶片可见旋转）
* 地面测试支架
* 工业照明（带阴影投射）

环境氛围：

* 工业风洞测试间风格
* 暗色基调 + 局部聚光
* 地面防滑网格、远处标尺、警示带
* 可选：工业风噪 / 风扇转动声

---

## 三、F-35 飞机建模

模型必须包含以下**外形特征**：

* 机头（含 EOTS / DAS 凸起传感器外形）
* 座舱盖（透明 + 内部座舱轮廓）
* 进气口（DSI 鼓包式无附面层隔道进气道）
* 主翼（梯形后掠）
* 水平尾翼（全动式，可联动偏转角 δe）
* 双垂直尾翼（外倾）
* 机身融合结构（升力体机身）
* 发动机尾喷口（菱形喷口轮廓）
* 起落架舱门外形（机腹 / 翼下）
* 机身主要表面分区（蒙皮接缝、检修口盖）

建模约束：

* 🚫 **禁止展示或推测任何非公开、机密或武器系统内部结构**
* 若无法加载外部 3D 模型，请使用**程序化几何体**构建具有**较高辨识度**的 F-35 外形
* 飞机表面应具有合理的**金属粗糙度、边缘高光、蒙皮分区和轻微使用痕迹**
* 提供 **LOD**：近处高模、远处低模
* 模型围绕重心轴线，可调整**俯仰 / 偏航 / 滚转**
* **可被点击拾取**（详见交互章节）

---

## 四、UI 整体框架（按截图布局）

整个 UI 分为 5 个区域：

| 区域 | 位置 | 内容 |
|------|------|------|
| **顶部菜单栏** | 上方居中 | 可视化模式 Tab · 相机预设下拉 · 隐藏面板 · 全屏 |
| **左侧控制面板** | 左侧 | 测试预设 + 风洞控制滑块 |
| **中央 3D 视口** | 中央 | Three.js 渲染画布（占满） |
| **右侧参数面板** | 右侧 | 实时气动参数（表盘 + 列表） |
| **底部状态栏** | 底部 | 免责 + 时间历程图表 + 操作按钮 + 操作提示 + 图例 |

视觉风格：深蓝灰 `#0E141B` 主色，琥珀 `#FFB300` + 青绿 `#00E5FF` 警示色，DIN 数字字体 + PingFang SC 中文字体，玻璃拟态面板。

---

## 五、可视化模式（顶部 Tab 栏，6 种 + 1 共 7 种）

切换可视化模式时，3D 视口立即重绘，对应渲染层叠加 / 替换。

### 1. 流线（Streamlines）
数千条动态流线展示空气从入口 → 绕机 → 扩散的过程。流线必须：

* 沿飞机外形发生偏移
* 随时间步进（粒子整体推进）
* **按"局部速度 / 来流速度"颜色映射**（图例颜色条 0.35–1.5，蓝→绿→黄→红）
* 边界层附近加密
* 涡流区域形成可见螺旋

### 2. 烟线（Smoke Lines）
类似风洞实验中注入烟的视觉效果：

* 白色 / 半透明烟带
* 比流线更宽、更柔和
* 浓度衰减

### 3. 粒子示踪（Particle Tracing）
密集粒子云：

* 5,000 ~ 100,000 粒子（受"粒子数量"滑块控制 10–100%）
* 每帧积分速度场更新位置
* 拖尾长度可选

### 4. 涡流（Vorticity）

* λ² 判据 或 Q 判据
* 翼尖涡、机背涡、进气道涡可视化
* 颜色按涡量强度映射

### 5. 压力云图（Pressure Heatmap）

* 飞机表面压力分布着色
* 🔵 蓝 = 吸力面低压
* 🔴 红 = 压力面高压
* 同步输出"最大/最小表面压力"数值

### 6. 速度云图（Velocity Heatmap）

* 风洞体空间半透明体积
* 颜色 ∝ 速度大小
* 可配合"截面切片"查看特定位置

### 7. 纹影（Schlieren）

* 模拟光学纹影成像
* 显示密度梯度
* 激波、边界层边缘清晰可见

---

## 六、风洞控制面板（左侧）

### 6.1 测试预设（10 个，2 列 × 5 行）

每个预设一键加载一组初始参数；选中预设后所有滑块联动更新。

| # | 名称 | 关键参数 |
|---|------|---------|
| 1 | 低速平飞 | U=60 m/s, α=3° |
| 2 | 高速平飞 | U=230 m/s, α=2° |
| 3 | 大攻角测试 | α=22° |
| 4 | 接近失速 | α=30° |
| 5 | 侧滑测试 | β=18° |
| 6 | 强湍流测试 | Tu=20% |
| 7 | 跨音速演示 | M=0.96 |
| 8 | 翼尖涡观察 | α=14° |
| 9 | 尾流分析 | 启用尾流截面 |
| 10 | 压力分布测试 | α=10° |

按钮行为：单击 → 应用预设参数；当前选中预设按钮高亮。

### 6.2 气流

| 参数 | 范围 | 默认 |
|------|------|------|
| 风速 | 0 – 500 m/s | 230 |

### 6.3 姿态

| 参数 | 范围 |
|------|------|
| 攻角 α | −10° ~ +35° |
| 侧滑角 β | −15° ~ +15° |
| **平尾偏转 δe** | −25° ~ +25° |

平尾偏转会**同时影响飞机水平尾翼的几何角度**和俯仰力矩。

### 6.4 大气

复选框：☑ **密度 / 粘度随高度温度联动**（默认勾选；勾选时海拔/温度修改密度与粘度，松绑则可独立设置）

| 参数 | 范围 |
|------|------|
| 海拔高度 | 0 – 12000 m |
| 空气温度 | 210 – 330 K |
| 空气密度 | 0.2 – 1.5 kg/m³ |
| 动力粘度 μ | 1.2 – 2.2 × 10⁻⁵ Pa·s |
| 末端湍流强度 Tu | 0 – 30 % |

### 6.5 模型

| 参数 | 范围 |
|------|------|
| **表面粗糙度** | 0 – 100 %（影响边界层厚度与 CL/CD） |

### 6.6 可视化

| 参数 | 范围 | 说明 |
|------|------|------|
| **粒子数量** | 10 – 100 % | 模式 3 有效 |
| **流线长度** | 4 – 40 段 | 模式 1 有效 |
| **可视化速度倍率** | 0.1 – 3 × | 时间推进速率（不改变物理） |

### 6.7 派生量（只读显示）

* 音速 a (m/s)
* 目标马赫数 M
* 静压 (kPa)

### 6.8 截面查看器

* **截面类型下拉**：速度云图 / 纹影
* **水平截面 XZ**（默认）
* **切片位置**：可在 −X ~ +X 范围内移动
* 切片位置变化时 3D 视口实时显示半透明平面

### 6.9 画质与性能

* 渲染分辨率倍率
* 抗锯齿开关
* 阴影开关
* 帧率限制（30/60/120/无限制）

---

## 七、实时气动参数面板（右侧）

### 7.1 顶部三表盘（圆形仪表）

风速 m/s、马赫数、攻角 ° —— 大字号 + 圆弧指示 + 数字。

### 7.2 详细参数列表（18 项）

| 中文 | 英文 / 单位 | 计算依据 |
|------|-------------|----------|
| 当前风速 | U, m/s | 用户输入 |
| 马赫数 | M | U / a(T) |
| 攻角 | α, ° | 用户输入 |
| 侧滑角 | β, ° | 用户输入 |
| 空气密度 | ρ, kg/m³ | ISA 模型（联动时） |
| 动压 | q, kPa | ½ρU² |
| 雷诺数（基于 MAC） | Re_MAC | ρUc/μ |
| 升力 | L, kN | 简化 CL·q·S |
| 阻力 | D, kN | 简化 CD·q·S |
| 侧向力 | Y, kN | 简化 CY·q·S |
| 升力系数 | CL | 查表 + 攻角 |
| 阻力系数 | CD | 查表 + 攻角 |
| 侧向力系数 | CY | 查表 + 侧滑 |
| 升阻比 | L/D | L / D |
| 俯仰力矩 | m, kN·m | 简化模型（含 δe） |
| 偏航力矩 | n, kN·m | 简化模型 |
| 滚转力矩 | l, kN·m | 简化模型 |
| 压力中心 | x/c | 0 ~ 1，沿 MAC |
| 估算失速裕度 | ° | α_max − 当前 α |
| 尾流湍流强度 | Tu_wake, % | 翼后 1 MAC 处估算 |
| 最大表面压力 | p_max, kPa | 压力云图采样 |
| 最低表面压力 | p_min, kPa | 压力云图采样 |

### 7.3 状态行

* **风洞工作状态**：风速调整中 / 稳态运行 / 待机
* **数据采样频率**：20 Hz（默认）
* **流场求解**：解析近似 / 数值求解（可切换）
* **单位制**：SI（国际单位制）

---

## 八、时间历程图表（底部）

6 个并排折线图，数据来源为参数面板的实时采样：

| # | 图表 | X 轴 | Y 轴 |
|---|------|------|------|
| 1 | 升力曲线 | 时间 (s) | 升力 (kN) |
| 2 | 阻力曲线 | 时间 (s) | 阻力 (kN) |
| 3 | CL – α 曲线 | 攻角 α (°) | CL |
| 4 | CD – α 曲线 | 攻角 α (°) | CD |
| 5 | 升阻比 | 时间 (s) | L/D |
| 6 | 压力中心 | 时间 (s) | x/c |

控制按钮：

* **暂停记录** —— 停止采样但保留数据
* **单步** —— 推进一帧
* **清空数据** —— 清空图表

---

## 九、交互控制

### 9.1 鼠标 / 触摸

| 输入 | 操作 |
|------|------|
| 左键拖拽 | 旋转视角 |
| 右键拖拽 / Shift + 左键 | 平移 |
| 滚轮 / 双指 | 缩放 |
| **单击飞机** | **显示该区域局部参数**（拾取点处压力 / 速度 / 涡量） |
| **双击飞机某部件** | **相机聚焦到该部件**（机翼、座舱、进气道、垂尾等） |

### 9.2 键盘

| 键 | 操作 |
|----|------|
| W / S | 俯仰 |
| A / D | 滚转 |
| Q / E | 偏航 |
| Space | 重置姿态 |
| 1 – 7 | 切换可视化模式 |
| R | 开始 / 停止录制 |
| C | 切换相机预设 |
| H | 隐藏 / 显示所有面板 |
| F | 全屏切换 |

---

## 十、相机系统

**预设视角**：

* 飞机侧视图（默认）
* 飞机俯视图、仰视图、正视图、后视图
* 伪座舱视角
* 跟随相机（飞机重心）
* 风洞全局

**自由相机**（OrbitControls），视角切换带 1 秒平滑过渡动画。

---

## 十一、教学辅助

* **部件高亮**：鼠标悬停时高亮（机翼、座舱、进气道、垂尾等）
* **标签悬浮**：部件名 + 简要描述
* **解释面板**：右侧 / 底部可展开，说明当前看到的空气动力学现象
* **预设场景**对应教学解释（见 6.1）

---

## 十二、物理模型

### 12.1 流场求解

* **解析近似**（默认，速度快）：基于势流理论 + 边界层修正 + 涡修正
* **数值求解**（可选，更精确）：Lattice Boltzmann 或简化 Navier-Stokes 在 GPU 上跑

### 12.2 气动力系数

CL、CD、CY 是关于 (α, β, δe, M, Re, Tu, 粗糙度) 的查表 / 拟合函数：

* 低速段：多项式拟合
* 跨音速段：激波诱导增量
* 大攻角：失速修正
* 平尾偏转：升降舵 → CL + Cm

### 12.3 大气模型

* ISA（国际标准大气）模型
* 联动时：海拔 + 温度 → 密度 / 音速 / 动力粘度
* 松绑时：用户直接输入密度 / 粘度

### 12.4 简化假设（必须在 UI 标注）

* **不可压缩 / 低速可压缩**（默认）
* 无粘性主导分离预测
* 忽略机身弹性形变
* 忽略发动机进气畸变

---

## 十三、性能与体验要求

* 桌面端：**60 FPS**（4K 屏）
* 移动端：**30+ FPS**（中等配置手机）
* 启动时间 ≤ 3 秒
* 首屏资源 gzip 后 ≤ 10 MB
* 浏览器：**Chrome 100+、Edge 100+、Firefox 100+、Safari 16+**
* 暗色主题，自适应窗口
* **多语言**：中文 / 英文切换

---

## 十四、工程结构

```
src/
├── scene/      # 风洞、飞机、灯光
├── flow/       # 流线、压力场、矢量场计算
├── ui/         # HUD、控制面板、教学面板
├── camera/     # 相机控制
├── physics/    # 简化气动力模型（CL/CD/L/D 计算）
├── aero/       # 大气模型、湍流模型
├── preset/     # 10 个测试预设
├── util/       # 数学、颜色、shader 编译
├── state/      # 全局状态（参数、模式、录制）
├── shaders/    # GLSL / WGSL 文件
└── i18n/       # 中英文文案
```

* TypeScript 优先
* 关键流体计算：WebGPU Compute Shader / WebGL 退化到 CPU

---

## 十五、视觉风格

* 工业风洞 + 高科技驾驶舱混合
* 主色：深蓝灰 `#0E141B`
* 警示色：琥珀 `#FFB300` / 青绿 `#00E5FF`
* 字体：DIN + PingFang SC
* 平面 UI：玻璃拟态 + 极简仪表

---

## 十六、合规与免责（必须）

UI 底部固定显示：

> ⚠️ 本模拟采用**简化空气动力学模型**，用于**教育和可视化展示**，**不可用于真实飞行测试、工程认证或性能评估**。

任何导出报告（PDF / 截图水印）必须携带该免责声明。

---

## 十七、测试与验收标准

* ✅ Chrome 最新版桌面端 ≥ 60 FPS
* ✅ iPhone 14 / Pixel 7 ≥ 30 FPS
* ✅ 10 个预设全部可一键加载
* ✅ 6 种可视化模式 + 纹影共 7 种可切换
* ✅ 大气"联动 / 独立"切换正确
* ✅ 平尾偏转 δe 实时影响水平尾翼 + CL + 俯仰力矩
* ✅ 单击飞机显示局部参数（压力 / 速度 / 涡量）
* ✅ 双击飞机部件聚焦相机
* ✅ 时间历程图表 6 条全部更新
* ✅ 录制 / 单步 / 清空数据按钮工作正常
* ✅ 隐藏面板 / 全屏 / 相机预设下拉均正常
* ✅ 免责文字始终可见
* ✅ 不依赖任何后端服务
* ✅ 不依赖外部 3D 模型（程序化几何）
* ✅ 代码可构建、可运行、首屏 ≤ 3 秒
* ✅ 飞机不展示任何机密 / 武器系统内部结构

---

## 十八、可选增强（超出截图）

* **多人协作**：同一会话，多人观察同一模拟
* **VR / AR 模式**：WebXR 支持沉浸式进入风洞
* **AI 教练**：用 LLM 解释当前现象、回答问题
* **数据回放**：录制后可在时间轴上回放 + 标注
* **导出报告**：PDF 实验报告（含截图 + 数据曲线 + 免责声明）
* **声音模拟**：风扇声、风切声、失速抖振声
* **多机型支持**：扩展到 F-22、Su-57、J-20 等（仍遵守"不展示内部结构"原则）
* **教学关卡**：新手引导分步教程
* **性能基准**：内置 FPS 监控 + 性能建议